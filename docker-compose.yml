services:
  cogstack-ui:
    build:
      context: apps/ui
      dockerfile: Dockerfile
    container_name: cogstack-ui
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    networks:
      - caddy-shared

networks:
  caddy-shared:
    external: true
