
```

---

### Step 1 — Notion schema introspection (PRD §6, first task)

Write `scripts/introspect_notion.py`. Connects to all 4 DBs, dumps property names/types/select options, writes `apps/ui/src/cogstack_ui/notion/schema.py` as typed Python constants (not Pydantic models — just plain dicts/dataclasses for zero runtime overhead). This runs once and commits the file.

**DBs covered:**

| DB | ID |
|---|---|
| B2C Leads | `32089024-cd3d-812e-a6c6-d8e21d9126b3` |
| B2C Batches | `32089024-cd3d-81a7-8691-ca999aa1494f` |
| B2B Leads | `30b89024-cd3d-810a-bb28-e46825320802` |
| Sources | `30b89024-cd3d-8142-a064-d40ed42a3443` |

Note: `notion_config.json` uses dashed IDs — PRD §6 uses compact IDs. I'll use the dashed versions from the config file as the authoritative source.

---

### Step 2 — `apps/ui/` scaffold

```
apps/ui/
├── pyproject.toml              # uv project: fastapi, jinja2, httpx, python-dotenv
├── Dockerfile                  # python:3.13-slim, uv install --no-dev, uvicorn
├── README.md
├── src/
│   └── cogstack_ui/
│       ├── __init__.py
│       ├── main.py             # FastAPI app, mounts routes, lifespan cache init
│       ├── config.py           # Pydantic BaseSettings (NOTION_API_KEY, DB IDs, PORT)
│       ├── cache.py            # Simple TTLCache (30s leads, 60s funnel) — no Redis
│       ├── notion/
│       │   ├── __init__.py
│       │   ├── client.py       # async httpx — query_database, retrieve_page
│       │   ├── schema.py       # generated by introspect_notion.py — property names/options
│       │   └── queries.py      # get_leads(), get_lead(), get_batches(), get_funnel_counts()
│       ├── routes/
│       │   ├── __init__.py
│       │   ├── dashboard.py    # GET /
│       │   ├── leads.py        # GET /leads, GET /leads/{id}
│       │   ├── batches.py      # GET /batches
│       │   ├── sources.py      # GET /sources
│       │   └── partials.py     # HTMX fragments: /partials/leads-table, /partials/funnel
│       └── templates/
│           ├── base.html       # Tailwind CDN, HTMX CDN, nav, flash messages
│           ├── dashboard.html
│           ├── leads/
│           │   ├── list.html
│           │   └── detail.html
│           ├── batches.html
│           ├── sources.html
│           └── partials/
│               ├── funnel.html
│               ├── leads_table.html
│               └── batch_row.html
└── tests/
    ├── conftest.py             # TestClient, mock Notion responses
    └── test_routes.py          # smoke tests: 200 on all 5 pages
```

**Dependencies** (`pyproject.toml`):
- `fastapi[standard]` (includes uvicorn + jinja2 + httpx)
- `python-dotenv`
- `cachetools` (TTLCache)

---

### Step 3 — `docker-compose.yml` (repo root, new file)

```yaml
services:
  leadgen-ui:
    build: apps/ui
    container_name: leadgen-ui
    restart: unless-stopped
    expose:
      - "8000"        # no published ports — Caddy reaches via caddy-shared
    env_file: .env
    networks:
      - caddy-shared

networks:
  caddy-shared:
    external: true
```

---

### Step 4 — Caddyfile addition

One new block appended to `/opt/infra/local-ai-packaged/Caddyfile`:

```caddyfile
# CogStack Leadgen UI
leads.bigtorig.com {
    encode zstd gzip
    basic_auth {
        claire <BCRYPT_HASH_PLACEHOLDER>
    }
    reverse_proxy leadgen-ui:8000
}
```

I'll write this as a **separate file** `deploy/Caddyfile.leads-block` in the repo for review/documentation. The actual Caddyfile edit on bigtorig is a one-liner you apply manually (or I do it with your confirmation).

---

### Page build order

| Order | Page | Route | Notes |
|---|---|---|---|
| 1 | Schema introspection | script | Foundation — run before templates |
| 2 | Notion client + queries | library | Data layer |
| 3 | Base template | shared | Layout, nav, Tailwind, HTMX |
| 4 | Dashboard | `/` | Funnel counts + attention panel |
| 5 | Leads list | `/leads` | Table + filters (status, source, province) |
| 6 | Lead detail | `/leads/{id}` | All properties + Notion deep link |
| 7 | Batches | `/batches` | Recent ingestion runs |
| 8 | Sources | `/sources` | Volume per source |
| 9 | Tests | — | Smoke tests, conftest mocks |

---

### What changes where

| File | Action |
|---|---|
| `apps/ui/` | **Create** (all new) |
| `docker-compose.yml` | **Create** at repo root |
| `deploy/Caddyfile.leads-block` | **Create** (Caddyfile snippet for review) |
| `scripts/introspect_notion.py` | **Create** |
| `/opt/infra/local-ai-packaged/Caddyfile` | **Edit** — one block appended (requires your confirmation) |
| All existing files | **Untouched** |

---

Ready to proceed on your approval. Two things to confirm before I start:
